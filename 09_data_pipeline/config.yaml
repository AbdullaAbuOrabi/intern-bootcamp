pipeline:
  name: week_9_full_data_pipeline
  retries: 3

paths:
  raw_data: data/raw
  processed_data: data/processed
  logs: logs
  validation_log: 09_data_pipeline/validation_log.csv
  summary_report: 09_data_pipeline/pipeline_run_summary.md

datasets:
  - customers
  - orders
  - order_items
  - products
  - transactions

validation:
  enabled: true

reporting:
  generate_summary: true